[project]
name = "hydra-typing"
version = "0.2.4"
description = "Typed dataclass configs for Hydra — so both you and your AI read the config easier"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [{ name = "Xinyu Zhang" }]
keywords = ["hydra", "typing", "dataclass", "configuration", "machine-learning", "yaml"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "hydra-core>=1.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.0",
]

[project.entry-points.hydra_plugins]
hydra_typing = "hydra_typing"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["hydra_typing"]
